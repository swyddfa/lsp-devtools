The `lsp-devtools record` command now accepts multiple format strings (e.g. `-f {message.method} -f {message.result}`). They will be tried in the order given, with messages using the format first string that applies cleanly.
